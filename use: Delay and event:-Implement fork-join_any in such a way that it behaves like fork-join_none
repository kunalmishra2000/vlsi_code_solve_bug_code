Implement fork-join_any in such a way that it behaves like fork-join_none 
use: Delay and event

module tab;
  event done;
  initial begin
    //-> done;
     fork 
  
       
          begin
            @done ;
          #2  $display("a");
          end
         begin
           @done;
          #4 $display("b");
         end
       
     begin
-> done;
     end
      
       join_any
    
    $display("exit");
       end
       endmodule
